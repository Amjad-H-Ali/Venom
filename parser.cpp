#include "AST.h"

// AST *parser::parse(AST *head) {

// 	if(*head == ast::BLOCK && *head->next == ast::LIST) {

// 		AST *block = parse(head->node->getValue());
// 		AST_Func(ast::FUNC, head->next, head)
// 	}
// };